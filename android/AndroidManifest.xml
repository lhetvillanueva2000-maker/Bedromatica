<?xml version="1.0" encoding="utf-8"?>
<!--
  No permissions are declared, on purpose.

  Worlds are read through the Storage Access Framework, which grants access to
  the one file the user picks. Structures are written into the shared Downloads
  collection, which needs no permission on Android 10+, and through a save
  dialog below that. There is no INTERNET permission either - every asset,
  including the fonts and the 3D library, ships inside the APK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.schembench"
    android:versionCode="3"
    android:versionName="1.2">

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:theme="@style/AppTheme"
      android:hardwareAccelerated="true"
      android:allowBackup="true"
      android:supportsRtl="true">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTop"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
